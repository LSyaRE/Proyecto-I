import { FiltroEquiposPipe } from './filtro-equipos.pipe';

describe('FiltroEquiposPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEquiposPipe();
    expect(pipe).toBeTruthy();
  });
});
